name: 데이터 갱신

on:
  schedule:
    - cron: '10 7 * * 1-5'
    - cron: '30 21 * * 1-5'
  workflow_dispatch:
  push:
    paths:
      - 'data/watchlist.json'
      - 'build_data.py'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 라이브러리 설치
        run: |
          python -m pip install --upgrade pip
          pip install pandas numpy finance-datareader yfinance pykrx lxml html5lib beautifulsoup4

      - name: 데이터 생성
        run: python build_data.py

      - name: 커밋
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/
          if git diff --staged --quiet; then
            echo "변경 없음"
          else
            git commit -m "데이터 갱신 $(TZ=Asia/Seoul date '+%Y-%m-%d %H:%M')"
            git push
          fi
